has_good_credit = True
has_criminal_record = False

if has_good_credit == True and not has_criminal_record == True:
    print(f"You are eligible for loan")
else:
    print(f"Sorry, we can't give you loan")
